---
title: Enterprise platform work
year: 2024–present
kind: full-time
status: client under NDA
summary: Two years shipping into a large, mature codebase I didn't write, for a Japanese client, on a bilingual team.
stack:
  - React
  - TypeScript
  - Node
  - Oracle
  - DynamoDB
  - AWS
scale: 206 merged PRs and 169 customer defects resolved since late 2024, most of them during UAT on a ~420-screen platform.
next: jf-and-the-world
---

<Callout>
No screenshots: the client and product are under NDA. What follows is the shape of the work, not the specifics.
</Callout>

## The short version

Side projects show what I can build from zero. This is the other half: working inside a system that's been running for years, with real users depending on it, where every change has to fit code and conventions that predate me.

## What I've worked on

**Legacy port: WinForms to React and Node on Oracle.** A desktop application being rebuilt as a web app, screen by screen, against an Oracle database that stayed put. The hard part isn't the React. It's reading the old code well enough to preserve behavior that nobody documented, deciding when to reproduce a quirk versus fix it, and doing that in a codebase where the answer to "how does this work" is often "ask the person who wrote it years ago" — in Japanese.

**Cross-stack defect work on a real estate management platform.** Bugs that land on my desk don't come labeled frontend or backend. A recent one: file uploads rejecting valid images. The validation logic was checking MIME types, and JPEGs arrive as several variants depending on the source. Tracing that from the user's report through the React upload flow to the validation rule, fixing it without loosening the check for everything else, and writing it up for a team that reads the ticket in a second language — that's a typical week.

**A QR-based parking system with realtime slot monitoring.** Slot events fan out over SNS and get processed off SQS, with DynamoDB holding slot state, so a scan at the gate doesn't wait on everything downstream. It was my first production message queue, and where I learned the difference between a queue and pub/sub by needing both.

**Developer tooling for the team, built on Claude Code.** I built the bug-fix pipeline the team runs on: scripts that pull tester reports from Google Sheets and Redmine, a structured analyze → fix → verify → request-review workflow as custom commands, staging log fetchers, evidence-video capture for reviewers, a small Postgres inventory for team metrics, and a dashboard that refreshes itself on EC2 four times a day. The interesting problem wasn't the prompts. It was deciding where an AI assistant is trustworthy in a codebase this size and where it isn't, then encoding those guardrails so the whole team gets them by default.

## What the environment taught me

**Working across a language barrier.** The CEO and most of the team communicate in Japanese. I've learned to write tickets, PR descriptions, and questions so they survive translation: short sentences, one idea each, screenshots over prose. It's made me a clearer communicator in English too.

**Reading before writing.** In a codebase this size, the most valuable skill is figuring out how something works before touching it. In practice that means git archaeology: reading the history of a line to decide whether what I'm looking at is a regression or a decision someone made on purpose. When there's no document, the legacy behavior is the spec until someone with authority says otherwise. Most of my time is spent tracing, not typing.

**Fix the pattern, not the instance.** Every fix ends with a sweep for the same defect elsewhere. The Japanese term is 横展開: roughly, "spread it sideways." A bug on one screen is usually sitting on the six screens the same person built the same month. Half my fixes now come from that sweep, not the original ticket.

**Small, defensible changes.** Big refactors in a system with years of accumulated behavior break things in ways you find out about a month later. I've learned to make the smallest change that fixes the problem, and to be able to explain exactly why it's safe.

<Callout>
In a mature codebase, the smallest change you can defend beats the cleanest change you can imagine.
</Callout>

## What I'd want next

This work taught me to be careful in a system where mistakes surface a month later. What I want next is a place where I can also be fast: a smaller team, more ownership of what gets built, and a shorter path from idea to shipped. That's why I'm looking at startups.
